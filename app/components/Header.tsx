import React from 'react';
import Navigation from './Navigation';

const Header = () => {
    return (
        <header className="w-full bg-white h-[80px] dark:bg-primaryD">
            <Navigation />
        </header>
    );
};

export default Header;
